<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Wild_Atlas.Views.SpeciesCheckForm"
             Title="SpeciesCheckForm">

    <VerticalStackLayout Padding="20" Spacing="10">

        <HorizontalStackLayout Spacing="8">
            <CheckBox x:Name="CbMammifere" />
            <Label Text="Mammifère" VerticalOptions="Center" />
        </HorizontalStackLayout>

        <HorizontalStackLayout Spacing="8">
            <CheckBox x:Name="CbOiseau" />
            <Label Text="Oiseau" VerticalOptions="Center" />
        </HorizontalStackLayout>

        <HorizontalStackLayout Spacing="8">
            <CheckBox x:Name="CbReptile" />
            <Label Text="Reptile" VerticalOptions="Center" />
        </HorizontalStackLayout>

    </VerticalStackLayout>

</ContentPage>